# Project-Scoped Rules

## Code Modification Scope
- You must make changes **in the frontend directory only** (`e:/Final-Xebia/frontend`).
- Do not modify files in the `Backend` directory or any other non-frontend directory unless explicitly instructed by the user.
